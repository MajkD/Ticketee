= @title = title(@project.name, "Projects")

= @project.name
= link_to "Edit Project", edit_project_path(@project)
= link_to "Delete Project", project_path(@project), method: :delete, data: {confirm: "Areyou sure you want to delete this project?"}
= @project.description